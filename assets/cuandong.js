const $=(s,r=document)=>r.querySelector(s); const $$=(s,r=document)=>[...r.querySelectorAll(s)];
function toast(msg){let t=$('.toast')||document.body.appendChild(Object.assign(document.createElement('div'),{className:'toast'}));t.textContent=msg;t.classList.add('show');setTimeout(()=>t.classList.remove('show'),2400)}
$$('[data-rfq]').forEach(b=>b.addEventListener('click',e=>{const p=e.currentTarget.dataset.rfq; localStorage.setItem('cuandong-rfq-product',p||''); toast((p||'Selection')+' added to RFQ draft'); setTimeout(()=>{ if(e.currentTarget.tagName==='BUTTON') location.href='contact.html';},650)}));
const form=$('#rfqForm'); if(form){ const saved=localStorage.getItem('cuandong-rfq-product'); if(saved&&$('#product')) $('#product').value=saved; const update=()=>{$$('#rfqForm input,#rfqForm select,#rfqForm textarea').forEach(i=>{const el=$('[data-sum="'+i.name+'"]'); if(el) el.textContent=i.value||'—'});}; form.addEventListener('input',update); update(); form.addEventListener('submit',e=>{e.preventDefault(); const id='RFQ-CU-'+new Date().getFullYear()+'-'+Math.floor(1000+Math.random()*8999); $('#confirm').innerHTML='<b>'+id+'</b><br>Draft received for export desk review. Submission is not a purchase order or binding offer.'; toast('RFQ draft submitted');});}
const search=$('#productSearch'); if(search){search.addEventListener('input',()=>{const q=search.value.toLowerCase(); $$('.product-row').forEach(r=>r.style.display=r.textContent.toLowerCase().includes(q)?'':'none')})}
